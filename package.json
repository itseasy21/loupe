{
  "name": "loupe-agent",
  "private": true,
  "version": "0.2.0",
  "description": "Release tooling for Loupe.",
  "scripts": {
    "changeset": "changeset",
    "version-packages": "changeset version && python scripts/version_sync.py sync && python scripts/version_sync.py check"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@types/node": "^20.14.10"
  }
}
